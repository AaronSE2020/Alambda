@*@page
@model AaronPortfolio.Views.Home.AboutModel
@{
    var messageList = HttpContext.Session.GetString("messages");
    List<string> messages = string.IsNullOrEmpty(messageList) ? new List<string>() : Newtonsoft.Json.JsonConvert.DeserializeObject<List<string>>(messageList);
}

<h2>About Page</h2>

<!-- Your existing content here -->
<!-- Input Notepad Section -->
<div id="notepad-section">
    <form method="post">
        <textarea name="newMessage" placeholder="Write a message..." rows="4" cols="50"></textarea>
        <button type="submit">Save Message</button>
    </form>

    <h3>Stored Messages:</h3>
    <ul>
        @foreach (var message in messages)
        {
            <li>@message</li>
        }
    </ul>
</div> *@